module importer_configs;

import common;

[cxximport("up::ImporterConfig", "potato/recon/importer.h")]
using ImporterConfig;

struct JsonImporterConfig : ImporterConfig {
    string type;
}

struct CopyImporterConfig : ImporterConfig {
    string type;
}
